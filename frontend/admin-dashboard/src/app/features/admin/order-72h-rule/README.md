# Deprecated — use `features/admin/operations/`

This folder is kept for:
- `order-72h-permissions.guard.ts` (used by operations routes)
- Legacy type exports consumed by older code

All admin operations UI lives in [`../operations/README.md`](../operations/README.md).
